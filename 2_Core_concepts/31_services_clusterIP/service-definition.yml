apiVersion: v1
kind: Service
metadata:
  name: back-end

spec: 
  type: ClusterIP
  ports:
    - targetPort: 80 # actual pod port
      port: 80 # service port

  selector:
    app: myapp
    type: back-end